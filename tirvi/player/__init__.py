"""tirvi.player — Python-side player feature stubs.

The interactive player is implemented in JavaScript under ``player/``.
This package holds Python stubs for deferred-MVP features that need a
named module + import path so traceability and tests can resolve.
"""
