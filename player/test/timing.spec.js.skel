// F35 — timing.js test skeleton (parses audio.json).
//
// Spec: N04/F35 DE-02. AC: US-01/AC-01.

import { describe, it, expect } from "@jest/globals";
import { parseAudioTimings } from "../js/timing.js";

describe("F35 timing.js", () => {
  describe("US-01/AC-01 — parseAudioTimings(audioJson)", () => {
    it.todo("validates schema: mark_id and start_s required");
    it.todo("sorts timings by start_s ascending");
    it.todo("end_s may be null (truncation tail per F30 DE-05)");
    it.todo("empty timings list returns empty array");
  });
});
