// F35 — player.js test skeleton (Jest/Vitest with jsdom).
//
// Spec: N04/F35 DE-01, DE-02. AC: US-01/AC-01. FT-anchors per F35 tasks.md.
//
// Toolchain not yet installed; this file is shape-only. Pre-`/tdd` setup
// will install Jest (or Vitest) + jsdom and rename to .test.js.

import { describe, it, expect } from "@jest/globals";
import { bootPlayer } from "../js/player.js";

describe("F35 player.js", () => {
  describe("US-01/AC-01 — bootPlayer loads page.json + audio.json", () => {
    it.todo("happy path: returns initialized PlayerState");
    it.todo("missing audio.json — degraded path: plays without highlight + banner (post-review C2)");
    it.todo("audio.json with error — surfaces non-blocking banner");
    it.todo("missing page.json — fails loud (cannot show page image)");
  });
});
