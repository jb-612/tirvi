// F35 — highlight.js test skeleton (rAF loop + bbox positioning).
//
// Spec: N04/F35 DE-03, DE-04, DE-05. AC: US-01/AC-01.

import { describe, it, expect } from "@jest/globals";
import { startHighlightLoop } from "../js/highlight.js";

describe("F35 highlight.js", () => {
  describe("US-01/AC-01 — rAF loop tracks audio.currentTime", () => {
    it.todo("active mark picked from sorted timings via binary search");
    it.todo("marker bbox updated to active word's bbox");
    it.todo("retina scale: image.naturalWidth vs rendered width");
    it.todo("loop stops on pause/ended");
  });
});
