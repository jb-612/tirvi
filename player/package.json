{
  "name": "tirvi-player",
  "version": "0.0.0",
  "description": "tirvi POC reader — vanilla HTML + Web Audio API (per ADR-023)",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.0.0",
    "jsdom": "^26.0.0"
  }
}
